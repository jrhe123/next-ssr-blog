import type { NextPage } from "next";
import Counter from "features/counter";

const Tag: NextPage = () => {
  return (
    <div>
      <p>tag page</p>
      <Counter />
    </div>
  );
};

export default Tag;
